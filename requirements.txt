# 도제학생 익명 신고 게시판 — 의존성

Django>=5.1,<5.2

# 5단계: 보안 설정
django-environ>=0.11        # .env 로 SECRET_KEY/DB 비밀번호 등 환경변수 관리
django-otp>=1.3             # 관리자(admin) 2단계 인증(MFA)
qrcode>=7.4                 # OTP 등록용 QR 코드 생성

# 배포(production) DB 드라이버 — production.py가 PostgreSQL을 사용
psycopg[binary]>=3.1

# 배포 앱 서버 (WSGI)
gunicorn>=21.2
